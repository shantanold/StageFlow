# PostgreSQL connection string
# With docker-compose: postgresql://stageflow:stageflow@localhost:5432/stageflow
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/stageflow"

# Random secret for signing JWTs — generate with: openssl rand -base64 32
JWT_SECRET="change-me-to-a-random-secret"

# Server port
PORT=3001
